# Task-Orbit-Smart-Task-Manager
Task Orbit is a beginner-friendly Task Management System built using Python. It allows users to add, view, update, and delete tasks directly from the command line. The project also stores tasks in a file, so data is saved even after closing the program.
